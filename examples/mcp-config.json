{
  "_comment": "Drop the mcpServers block into any MCP client that speaks Streamable HTTP. No credentials are required for the eleven free tools. If your client only launches subprocesses, use the stdio entry instead: it runs bridge/dfx_mcp_stdio.py from this repository, which forwards to the same URL and needs nothing installed.",
  "mcpServers": {
    "dfx-real-estate": {
      "type": "http",
      "url": "https://exchange-production-9123.up.railway.app/mcp"
    },
    "dfx-real-estate-stdio": {
      "command": "python3",
      "args": [
        "bridge/dfx_mcp_stdio.py"
      ]
    }
  }
}
