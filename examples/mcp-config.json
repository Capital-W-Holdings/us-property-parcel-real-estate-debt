{
  "_comment": "Drop the mcpServers block into any MCP client that speaks Streamable HTTP. No credentials are required for the eleven free tools. If your client only launches subprocesses, use the stdio entry instead and replace /path/to with wherever you cloned this repository: an MCP client launches the command from its own working directory, not yours, so a relative path will not find the file.",
  "mcpServers": {
    "dfx-real-estate": {
      "type": "http",
      "url": "https://exchange-production-9123.up.railway.app/mcp"
    },
    "dfx-real-estate-stdio": {
      "command": "python3",
      "args": [
        "/path/to/us-property-parcel-real-estate-debt/bridge/dfx_mcp_stdio.py"
      ]
    }
  }
}
